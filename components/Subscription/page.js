import React from 'react';
import SubscriptionCard from './SubscriptionCard';

const page = () => {
    return (
        <div>
            <SubscriptionCard/>
        </div>
    );
};

export default page;